class AppConstants {
  AppConstants._();

  static const String appName = 'Quick PDF Tool';
  static const String outputFolderName = 'quick_pdf_tool';

  static const Duration snackBarDuration = Duration(seconds: 4);
}
